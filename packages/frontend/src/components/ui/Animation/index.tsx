import { CSSProperties } from "react"

// no clue wtf is this xD

interface OwnProps {
    style: CSSProperties
}

export const Animation:React.FC<OwnProps> = ({style}) => {
    return (
        <div>KEK</div>
    )
}